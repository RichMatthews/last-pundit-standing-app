{
  "rules": {
    "users": {
      "$uid": {
        ".read": "$uid === auth.uid",
        ".write": "$uid === auth.uid"
      }
    },
    "leagues": {
       ".read": "auth.uid !=null",
       ".write": "auth.uid !=null"
    },
    "information": {
       ".read": "auth.uid !=null",
       ".write": "auth.uid !=null"
    }
  }
}




{
  "rules": {
    ".read": true,
    ".write": true
  }
}